import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    centerContent : {
        flex: 1
    },
    centerGrid : {
        alignItems: 'center',
        justifyContent: 'center'
    }

});